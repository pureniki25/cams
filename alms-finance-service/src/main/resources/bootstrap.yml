server:
  port: 30621
spring:
  application:
    name: alms-finance-service
  cloud:
    config:
      #name: alms-core-service,base,rabbitmq,mongo,redis
      discovery:
        service-id: ussp-config-app
        enabled: false
      enabled: false
    bus:
      enabled: false
eureka:
  instance:
    prefer-ip-address: true
  client:
      serviceUrl:
        defaultZone: http://192.168.15.89:30101/eureka/  # 配置注册中心url
#        defaultZone: http://172.16.200.110:30101/eureka/  # 配置注册中心url
ribbon:
  ReadTimeout: 60000
  ConnectTimeout: 60000