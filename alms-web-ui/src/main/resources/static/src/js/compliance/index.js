var basePath;
var vm;

window.layinit(function(htConfig) {
	basePath = htConfig.financeBasePath;

	vm = new Vue({
		el : '#app',
		data : {

		},

		methods : {

		},

		created : function() {
		},

	});

});
