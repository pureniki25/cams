var basePath;
var vm;

//设置表单验证

window.layinit(function (htConfig) {
    basePath = htConfig.coreBasePath;
    
	vm = new Vue({
	   el: '#app',
	   data: {
	    	
	    	
	        },
	        
	       
	   methods: {
		   
	   },
	   
	   created:function(){
		   
	   },
	   
	});
    
});
