server:
  port: 30606

spring:
  application:
    name: alms-core-service
  cloud:
    config:
      name: alms-core-service,base,rabbitmq,mongo,redis
      discovery:
        service-id: ussp-config-app
        enabled: false
      enabled: false
    bus:
      enabled: false
eureka:
  instance:
    prefer-ip-address: true
  client:
      serviceUrl:
#        defaultZone: http://192.168.12.101:30101/eureka/  # 配置注册中心url
        defaultZone: http://172.16.200.110:30101/eureka/  # 配置注册中心url

